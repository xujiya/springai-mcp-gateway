server:
  port: 9092

spring:
  application:
    name: weather-server
  ai:
    mcp:
      server:
        name: weather-server
        version: 1.0.0
        protocol: STREAMABLE

logging:
  level:
    es.omarall: DEBUG
    io.modelcontextprotocol: INFO
    org.springframework.ai: INFO
